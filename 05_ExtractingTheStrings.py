name = "Daravuth"

# The below code will extract data from the 0th index until the 3rd index
print(name[0:4])
print(name[:4])

# The below code will extract data from the 4th index until the last index
print(name[4:8])
print(name[4:])

# The below code will extract data from the 0th index until the last index
print(name)
print(name[:])